- Add Test262-derived executable windows for `QuickBEAM.JS.BytecodeCompiler` after harness/module/async filtering is explicit; use QuickJS as oracle and avoid noisy monolithic sweeps.
- Expand bytecode writer roundtrips for advanced class/prototype semantics, try/finally, and closure metadata as those constructs move beyond the current simplified subsets.
- Real closure capture metadata is unsafe with the current writer/interpreter assumptions: two attempts at emitting var_ref/closure_var metadata for immediate parent captures segfaulted stable frontend native-load validation. Closure work must start from decoded QuickJS closure bytecode writer roundtrips before emitting new closure metadata.
- Remaining full-parity scoping work after frontier fixes: real lexical environments, TDZ, per-iteration environments, and block/function declaration edge cases beyond the current simple var-hoist/block-let isolation subset.
