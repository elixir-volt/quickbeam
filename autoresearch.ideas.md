# Autoresearch Ideas — Web API Builtins

## Implementation approach
Each API is a builtin registered in Runtime.Globals. Use existing Elixir implementations where possible (URI for URL, :crypto for crypto, :base64 for atob/btoa).
